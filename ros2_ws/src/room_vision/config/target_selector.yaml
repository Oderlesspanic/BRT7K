room_vision_target_selector:
  ros__parameters:
    detections_topic: "/room_vision/detections"
    camera_info_topic: "/camera/camera_info"
    image_topic: "/camera/image_raw"
    target_topic: "/room_vision/target"
    target_image_topic: "/room_vision/target_annotated"
    filter_classes: ""                          # Komma-getrennt, leer = alle: z.B. "wurfel,ball"
    deadzone_px: 40
    distance_k: 8000.0
    publish_no_target: false
    publish_annotated_image: true
